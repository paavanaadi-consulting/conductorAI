# =============================================================================
# ConductorAI - Secret
# =============================================================================
# Sensitive credentials for ConductorAI.
#
# IMPORTANT: Replace the placeholder values below before applying.
#
#   echo -n "sk-your-openai-api-key" | base64
#   echo -n "your-redis-password"    | base64
#
# For production, manage secrets with a tool like:
#   - Sealed Secrets (Bitnami)
#   - External Secrets Operator
#   - HashiCorp Vault
#   - AWS Secrets Manager / GCP Secret Manager
#
# Do NOT commit real credentials to version control.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: conductorai-secrets
  namespace: conductorai
  labels:
    app: conductorai
type: Opaque
data:
  # Base64-encoded placeholders -- replace before applying
  # echo -n "REPLACE_WITH_OPENAI_API_KEY" | base64
  CONDUCTOR_LLM__API_KEY: UkVQTEFDRV9XSVRIX09QRU5BSV9BUElfS0VZ
  # echo -n "REPLACE_WITH_REDIS_PASSWORD" | base64
  CONDUCTOR_REDIS__PASSWORD: UkVQTEFDRV9XSVRIX1JFRElTX1BBU1NXT1JE
